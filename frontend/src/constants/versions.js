/**
 * Version constants with independent lifecycles:
 * - APP version is managed from repo root VERSION and synced into manifests/runtime metadata.
 * - EXPORT_SCHEMA_VERSION tracks JSON import/export structure compatibility.
 */
export const EXPORT_SCHEMA_VERSION = '1.0.0';
